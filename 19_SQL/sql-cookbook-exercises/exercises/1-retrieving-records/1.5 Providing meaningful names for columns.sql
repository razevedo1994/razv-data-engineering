/*
 * Problem: You would like to change the names of the columns that are returned by your 
 * query so they are more readable and understandable.
 * 
 */

select  sal as salary,
        comm as commission
from emp