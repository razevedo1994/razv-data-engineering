/*
 * Problem: You have a table and want to see values for specific columns rather than for all the columns.
 *
 * For example: To see only name, departament number and salary for employees.
 * 
 */

select  ename, 
        deptno, 
        sal
from emp